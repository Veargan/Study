class xPerson
{
	static PersonDAO getInstance()
	{
		PersonDAO pd = null;
		
		switch (type) {
			case "Mongo": pd = new PersonDAO_MongoDB(); break;
		}
		
		return pd;
	}
}


Type tt = p.GetType();
string res = tt.Name + "/r/n";

FieldInfo[] fi = tt.GetFields();
foreach ( in tt)
{
	res +="/t" + f.Name + "" + f.GetValue(p).ToString() + f.Name  + "";
}